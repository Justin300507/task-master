# Task Master

> Generated by **ForgeAI** — AI-powered full-stack application generator.

## Features

- Create, read, update, delete, and complete tasks with optional tags and due dates.
- Secure sign‑up, login, JWT handling, and role‑based access for owners vs collaborators.
- Server‑side filtering by status, due date, tags, free‑text search, and paginated results.

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Backend | FastAPI |
| Database | SQLite |
| Frontend | React + Vite |
| Auth | JWT Bearer tokens |

## Quick Start

### Prerequisites

- Python 3.11+
- Node.js 18+ (for frontend)

### Backend

```bash
cd app
pip install -r requirements.txt
uvicorn main:app --reload
```

API available at: `http://localhost:8000`
Interactive docs: `http://localhost:8000/docs`

### Frontend

```bash
npm install
npm run dev
```

Frontend at: `http://localhost:5173`

### Docker

```bash
docker build -t task_master .
docker run -p 8000:8000 task_master
```

## API Reference

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/auth/signup` | Signup |
| `POST` | `/auth/login` | Login |
| `GET` | `/auth/me` | Me |
| `GET` | `/lists` | Get Lists |
| `GET` | `/lists/{id}` | Get List |
| `POST` | `/lists` | Create List |
| `PUT` | `/lists/{id}` | Update List |
| `DELETE` | `/lists/{id}` | Delete List |
| `GET` | `/list_members` | List Members |
| `GET` | `/list_members/{member_id}` | Get List Member |
| `POST` | `/list_members` | Create List Member |
| `PUT` | `/list_members/{member_id}` | Update List Member |
| `DELETE` | `/list_members/{member_id}` | Delete List Member |
| `POST` | `/seed` | Seed Data |
| `GET` | `/stats/summary` | Get Summary |
| `GET` | `/tags` | List Tags |
| `GET` | `/tags/{tag_id}` | Get Tag |
| `POST` | `/tags` | Create Tag |
| `PUT` | `/tags/{tag_id}` | Update Tag |
| `DELETE` | `/tags/{tag_id}` | Delete Tag |


## Database Schema

6 table(s): ListMember, List, Tag, TaskTag, Task, User

See [ARCHITECTURE.md](ARCHITECTURE.md) for full ER diagram.

## Project Structure

```
app/
├── main.py          # FastAPI entry point
├── database.py      # DB connection & session
├── models/          # SQLAlchemy ORM models
├── schemas/         # Pydantic request/response schemas
├── routes/          # API route handlers
├── services/        # Business logic
└── requirements.txt
src/                 # React frontend
```

---
_Built with [ForgeAI](https://github.com/forgeai) · 31 endpoints · 6 models_
